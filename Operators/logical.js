// Logical --> &&,||,!
let a = 30;
let b = 30;
let c = 10;

console.log((a == b) && (b != c));// both the condition is true , then its gives as true
console.log((a == b) || (b != c)); // any one of the condition is true, or both of them are 
// true then it returns true